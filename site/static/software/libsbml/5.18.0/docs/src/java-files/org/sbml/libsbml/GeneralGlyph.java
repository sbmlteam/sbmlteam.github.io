/* ----------------------------------------------------------------------------
 * This file was automatically generated by SWIG (http://www.swig.org).
 * Version 3.0.12
 *
 * Do not make changes to this file unless you know what you are doing--modify
 * the SWIG interface file instead.
 * ----------------------------------------------------------------------------- */

package org.sbml.libsbml;

/** 
 * <span class="pkg-marker pkg-color-layout"><a href="group__layout.html">layout</a></span>

 A glyph for miscellaneous items.
 <p>
 * The {@link GeneralGlyph} is used to facilitate the representation of elements
 * other than {@link Compartment}, {@link Species} and {@link Reaction} and thus can be used for the
 * display of relationships of {@link Rule} or elements defined by other SBML
 * packages. It closely follows the structure of the {@link ReactionGlyph}.
 * {@link GeneralGlyph} is defined to have an optional attribute reference as well as
 * the elements curve, listOfReferenceGlyphs and listOfSubGlyphs.
 */

public class GeneralGlyph extends GraphicalObject {
   private long swigCPtr;

   protected GeneralGlyph(long cPtr, boolean cMemoryOwn)
   {
     super(libsbmlJNI.GeneralGlyph_SWIGUpcast(cPtr), cMemoryOwn);
     swigCPtr = cPtr;
   }

   protected static long getCPtr(GeneralGlyph obj)
   {
     return (obj == null) ? 0 : obj.swigCPtr;
   }

   protected static long getCPtrAndDisown (GeneralGlyph obj)
   {
     long ptr = 0;

     if (obj != null)
     {
       ptr             = obj.swigCPtr;
       obj.swigCMemOwn = false;
     }

     return ptr;
   }

  protected void finalize() {
    delete();
  }

  public synchronized void delete() {
    if (swigCPtr != 0) {
      if (swigCMemOwn) {
        swigCMemOwn = false;
        libsbmlJNI.delete_GeneralGlyph(swigCPtr);
      }
      swigCPtr = 0;
    }
    super.delete();
  }

  
/**
   * Creates a new {@link GeneralGlyph}.  The list of reference glyph and subglyphs is
   * empty and the id of the associated element is set to the empty
   * string.
   <p>
   * @param level the SBML Level.
   * @param version the Version within the SBML Level.
   * @param pkgVersion the version of the package.
   <p>
   * <p>
 * @note Attempting to add an object to an {@link SBMLDocument} having a different
 * combination of SBML Level, Version and XML namespaces than the object
 * itself will result in an error at the time a caller attempts to make the
 * addition.  A parent object must have compatible Level, Version and XML
 * namespaces.  (Strictly speaking, a parent may also have more XML
 * namespaces than a child, but the reverse is not permitted.)  The
 * restriction is necessary to ensure that an SBML model has a consistent
 * overall structure.  This requires callers to manage their objects
 * carefully, but the benefit is increased flexibility in how models can be
 * created by permitting callers to create objects bottom-up if desired.  In
 * situations where objects are not yet attached to parents (e.g.,
 * {@link SBMLDocument}), knowledge of the intented SBML Level and Version help
 * libSBML determine such things as whether it is valid to assign a
 * particular value to an attribute.  For packages, this means that the 
 * parent object to which this package element is being added must have
 * been created with the package namespace, or that the package namespace
 * was added to it, even if that parent is not a package object itself.
   */ public
 GeneralGlyph(long level, long version, long pkgVersion) throws org.sbml.libsbml.SBMLConstructorException {
    this(libsbmlJNI.new_GeneralGlyph__SWIG_0(level, version, pkgVersion), true);
  }

  
/**
   * Creates a new {@link GeneralGlyph}.  The list of reference glyph and subglyphs is
   * empty and the id of the associated element is set to the empty
   * string.
   <p>
   * @param level the SBML Level.
   * @param version the Version within the SBML Level.
   * @param pkgVersion the version of the package.
   <p>
   * <p>
 * @note Attempting to add an object to an {@link SBMLDocument} having a different
 * combination of SBML Level, Version and XML namespaces than the object
 * itself will result in an error at the time a caller attempts to make the
 * addition.  A parent object must have compatible Level, Version and XML
 * namespaces.  (Strictly speaking, a parent may also have more XML
 * namespaces than a child, but the reverse is not permitted.)  The
 * restriction is necessary to ensure that an SBML model has a consistent
 * overall structure.  This requires callers to manage their objects
 * carefully, but the benefit is increased flexibility in how models can be
 * created by permitting callers to create objects bottom-up if desired.  In
 * situations where objects are not yet attached to parents (e.g.,
 * {@link SBMLDocument}), knowledge of the intented SBML Level and Version help
 * libSBML determine such things as whether it is valid to assign a
 * particular value to an attribute.  For packages, this means that the 
 * parent object to which this package element is being added must have
 * been created with the package namespace, or that the package namespace
 * was added to it, even if that parent is not a package object itself.
   */ public
 GeneralGlyph(long level, long version) throws org.sbml.libsbml.SBMLConstructorException {
    this(libsbmlJNI.new_GeneralGlyph__SWIG_1(level, version), true);
  }

  
/**
   * Creates a new {@link GeneralGlyph}.  The list of reference glyph and subglyphs is
   * empty and the id of the associated element is set to the empty
   * string.
   <p>
   * @param level the SBML Level.
   * @param version the Version within the SBML Level.
   * @param pkgVersion the version of the package.
   <p>
   * <p>
 * @note Attempting to add an object to an {@link SBMLDocument} having a different
 * combination of SBML Level, Version and XML namespaces than the object
 * itself will result in an error at the time a caller attempts to make the
 * addition.  A parent object must have compatible Level, Version and XML
 * namespaces.  (Strictly speaking, a parent may also have more XML
 * namespaces than a child, but the reverse is not permitted.)  The
 * restriction is necessary to ensure that an SBML model has a consistent
 * overall structure.  This requires callers to manage their objects
 * carefully, but the benefit is increased flexibility in how models can be
 * created by permitting callers to create objects bottom-up if desired.  In
 * situations where objects are not yet attached to parents (e.g.,
 * {@link SBMLDocument}), knowledge of the intented SBML Level and Version help
 * libSBML determine such things as whether it is valid to assign a
 * particular value to an attribute.  For packages, this means that the 
 * parent object to which this package element is being added must have
 * been created with the package namespace, or that the package namespace
 * was added to it, even if that parent is not a package object itself.
   */ public
 GeneralGlyph(long level) throws org.sbml.libsbml.SBMLConstructorException {
    this(libsbmlJNI.new_GeneralGlyph__SWIG_2(level), true);
  }

  
/**
   * Creates a new {@link GeneralGlyph}.  The list of reference glyph and subglyphs is
   * empty and the id of the associated element is set to the empty
   * string.
   <p>
   * @param level the SBML Level.
   * @param version the Version within the SBML Level.
   * @param pkgVersion the version of the package.
   <p>
   * <p>
 * @note Attempting to add an object to an {@link SBMLDocument} having a different
 * combination of SBML Level, Version and XML namespaces than the object
 * itself will result in an error at the time a caller attempts to make the
 * addition.  A parent object must have compatible Level, Version and XML
 * namespaces.  (Strictly speaking, a parent may also have more XML
 * namespaces than a child, but the reverse is not permitted.)  The
 * restriction is necessary to ensure that an SBML model has a consistent
 * overall structure.  This requires callers to manage their objects
 * carefully, but the benefit is increased flexibility in how models can be
 * created by permitting callers to create objects bottom-up if desired.  In
 * situations where objects are not yet attached to parents (e.g.,
 * {@link SBMLDocument}), knowledge of the intented SBML Level and Version help
 * libSBML determine such things as whether it is valid to assign a
 * particular value to an attribute.  For packages, this means that the 
 * parent object to which this package element is being added must have
 * been created with the package namespace, or that the package namespace
 * was added to it, even if that parent is not a package object itself.
   */ public
 GeneralGlyph() throws org.sbml.libsbml.SBMLConstructorException {
    this(libsbmlJNI.new_GeneralGlyph__SWIG_3(), true);
  }

  
/**
   * Creates a new {@link GeneralGlyph} with the given {@link LayoutPkgNamespaces} object.
   <p>
   * <p>
 * The package namespaces object used in this constructor is derived from a
 * {@link SBMLNamespaces} object, which encapsulates SBML Level/Version/namespaces
 * information.  It is used to communicate the SBML Level, Version, and 
 * package version and name information used in addition to SBML Level&nbsp;3 Core.  A
 * common approach to using libSBML's {@link SBMLNamespaces} facilities is to create an
 * package namespace object somewhere in a program once, then hand that object
 * as needed to object constructors of that package that accept it as and
 * argument, such as this one.
   <p>
   * @param layoutns the {@link LayoutPkgNamespaces} object.
   <p>
   * <p>
 * @note Attempting to add an object to an {@link SBMLDocument} having a different
 * combination of SBML Level, Version and XML namespaces than the object
 * itself will result in an error at the time a caller attempts to make the
 * addition.  A parent object must have compatible Level, Version and XML
 * namespaces.  (Strictly speaking, a parent may also have more XML
 * namespaces than a child, but the reverse is not permitted.)  The
 * restriction is necessary to ensure that an SBML model has a consistent
 * overall structure.  This requires callers to manage their objects
 * carefully, but the benefit is increased flexibility in how models can be
 * created by permitting callers to create objects bottom-up if desired.  In
 * situations where objects are not yet attached to parents (e.g.,
 * {@link SBMLDocument}), knowledge of the intented SBML Level and Version help
 * libSBML determine such things as whether it is valid to assign a
 * particular value to an attribute.  For packages, this means that the 
 * parent object to which this package element is being added must have
 * been created with the package namespace, or that the package namespace
 * was added to it, even if that parent is not a package object itself.
   */ public
 GeneralGlyph(LayoutPkgNamespaces layoutns) throws org.sbml.libsbml.SBMLConstructorException {
    this(libsbmlJNI.new_GeneralGlyph__SWIG_4(LayoutPkgNamespaces.getCPtr(layoutns), layoutns), true);
  }

  
/**
   * Creates a glyph with the given {@link LayoutPkgNamespaces} and id.
   <p>
   * (FOR BACKWARD COMPATIBILITY)
   */ public
 GeneralGlyph(LayoutPkgNamespaces layoutns, String id) throws org.sbml.libsbml.SBMLConstructorException {
    this(libsbmlJNI.new_GeneralGlyph__SWIG_5(LayoutPkgNamespaces.getCPtr(layoutns), layoutns, id), true);
  }

  
/**
   * Creates a glyph with the given {@link LayoutPkgNamespaces}, id and set the id of the
   * associated element to the second argument.
   <p>
   * (FOR BACKWARD COMPATIBILITY)
   */ public
 GeneralGlyph(LayoutPkgNamespaces layoutns, String id, String referenceId) throws org.sbml.libsbml.SBMLConstructorException {
    this(libsbmlJNI.new_GeneralGlyph__SWIG_6(LayoutPkgNamespaces.getCPtr(layoutns), layoutns, id, referenceId), true);
  }

  
/**
   * Creates a new {@link GeneralGlyph} from the given {@link XMLNode}
   <p>
   * (FOR BACKWARD COMPATIBILITY)
   */ public
 GeneralGlyph(XMLNode node, long l2version) throws org.sbml.libsbml.SBMLConstructorException {
    this(libsbmlJNI.new_GeneralGlyph__SWIG_7(XMLNode.getCPtr(node), node, l2version), true);
  }

  
/**
   * Creates a new {@link GeneralGlyph} from the given {@link XMLNode}
   <p>
   * (FOR BACKWARD COMPATIBILITY)
   */ public
 GeneralGlyph(XMLNode node) throws org.sbml.libsbml.SBMLConstructorException {
    this(libsbmlJNI.new_GeneralGlyph__SWIG_8(XMLNode.getCPtr(node), node), true);
  }

  
/**
   * Copy constructor.
   <p>
   * @param source the instance to copy.
   */ public
 GeneralGlyph(GeneralGlyph source) throws org.sbml.libsbml.SBMLConstructorException {
    this(libsbmlJNI.new_GeneralGlyph__SWIG_9(GeneralGlyph.getCPtr(source), source), true);
  }

  
/**
   * <p>
 * Replaces all uses of a given <code>SIdRef</code> type attribute value with another
 * value.
 <p>
 * <p>
 * In SBML, object identifiers are of a data type called <code>SId</code>.
 * In SBML Level&nbsp;3, an explicit data type called <code>SIdRef</code> was
 * introduced for attribute values that refer to <code>SId</code> values; in
 * previous Levels of SBML, this data type did not exist and attributes were
 * simply described to as 'referring to an identifier', but the effective
 * data type was the same as <code>SIdRef</code> in Level&nbsp;3.  These and
 * other methods of libSBML refer to the type <code>SIdRef</code> for all
 * Levels of SBML, even if the corresponding SBML specification did not
 * explicitly name the data type.
 <p>
 * This method works by looking at all attributes and (if appropriate)
 * mathematical formulas in MathML content, comparing the referenced
 * identifiers to the value of <code>oldid</code>.  If any matches are found, the
 * matching values are replaced with <code>newid</code>.  The method does <em>not</em>
 * descend into child elements.
 <p>
 * @param oldid the old identifier.
 * @param newid the new identifier.
   */ public
 void renameSIdRefs(String oldid, String newid) {
    libsbmlJNI.GeneralGlyph_renameSIdRefs(swigCPtr, this, oldid, newid);
  }

  
/**
   * Returns the id of the associated element.
   */ public
 String getReferenceId() {
    return libsbmlJNI.GeneralGlyph_getReferenceId(swigCPtr, this);
  }

  
/**
   * Sets the id of the associated element.
   */ public
 int setReferenceId(String id) {
    return libsbmlJNI.GeneralGlyph_setReferenceId(swigCPtr, this, id);
  }

  
/**
   * Returns <code>true</code> if the id of the associated element is not the empty
   * string.
   */ public
 boolean isSetReferenceId() {
    return libsbmlJNI.GeneralGlyph_isSetReferenceId(swigCPtr, this);
  }

  
/**
   * Returns the {@link ListOf} object that hold the reference glyphs.
   */ public
 ListOfReferenceGlyphs getListOfReferenceGlyphs() {
    long cPtr = libsbmlJNI.GeneralGlyph_getListOfReferenceGlyphs__SWIG_0(swigCPtr, this);
    return (cPtr == 0) ? null : new ListOfReferenceGlyphs(cPtr, false);
  }

  
/**
   * Returns the {@link ListOf} object that hold the sub glyphs.
   */ public
 ListOfGraphicalObjects getListOfSubGlyphs() {
    long cPtr = libsbmlJNI.GeneralGlyph_getListOfSubGlyphs__SWIG_0(swigCPtr, this);
    return (cPtr == 0) ? null : new ListOfGraphicalObjects(cPtr, false);
  }

  
/**
   * Returns the reference glyph with the given <code>index</code>.
   * If the index is invalid, <code>null</code> is returned.
   */ public
 ReferenceGlyph getReferenceGlyph(long index) {
    long cPtr = libsbmlJNI.GeneralGlyph_getReferenceGlyph__SWIG_0(swigCPtr, this, index);
    return (cPtr == 0) ? null : new ReferenceGlyph(cPtr, false);
  }

  
/**
   * Returns the sub glyph with the given <code>index</code>.
   * If the index is invalid, <code>null</code> is returned.
   */ public
 GraphicalObject getSubGlyph(long index) {
	return (GraphicalObject) libsbml.DowncastSBase(libsbmlJNI.GeneralGlyph_getSubGlyph__SWIG_0(swigCPtr, this, index), false);
}

  
/**
   * Adds a new reference glyph to the list.
   */ public
 int addReferenceGlyph(ReferenceGlyph glyph) {
    return libsbmlJNI.GeneralGlyph_addReferenceGlyph(swigCPtr, this, ReferenceGlyph.getCPtr(glyph), glyph);
  }

  
/**
   * Adds a new subglyph to the list.
   */ public
 int addSubGlyph(GraphicalObject glyph) {
    return libsbmlJNI.GeneralGlyph_addSubGlyph(swigCPtr, this, GraphicalObject.getCPtr(glyph), glyph);
  }

  
/**
   * Returns the number of reference glyph objects.
   */ public
 long getNumReferenceGlyphs() {
    return libsbmlJNI.GeneralGlyph_getNumReferenceGlyphs(swigCPtr, this);
  }

  
/**
   * Returns the number of subglyph objects.
   */ public
 long getNumSubGlyphs() {
    return libsbmlJNI.GeneralGlyph_getNumSubGlyphs(swigCPtr, this);
  }

  
/**
   * Calls initDefaults from {@link GraphicalObject}.
   */ public
 void initDefaults() {
    libsbmlJNI.GeneralGlyph_initDefaults(swigCPtr, this);
  }

  
/**
   * Returns the curve object for the reaction glyph
   */ public
 Curve getCurve() {
    long cPtr = libsbmlJNI.GeneralGlyph_getCurve__SWIG_0(swigCPtr, this);
    return (cPtr == 0) ? null : new Curve(cPtr, false);
  }

  
/**
   * Sets the curve object for the reaction glyph.
   */ public
 void setCurve(Curve curve) {
    libsbmlJNI.GeneralGlyph_setCurve(swigCPtr, this, Curve.getCPtr(curve), curve);
  }

  
/**
   * Returns <code>true</code> if the curve consists of one or more segments.
   */ public
 boolean isSetCurve() {
    return libsbmlJNI.GeneralGlyph_isSetCurve(swigCPtr, this);
  }

  
/** */ public
 boolean getCurveExplicitlySet() {
    return libsbmlJNI.GeneralGlyph_getCurveExplicitlySet(swigCPtr, this);
  }

  
/**
   * Creates a new {@link ReferenceGlyph} object, adds it to the end of the
   * list of reference objects and returns a reference to the newly
   * created object.
   */ public
 ReferenceGlyph createReferenceGlyph() {
    long cPtr = libsbmlJNI.GeneralGlyph_createReferenceGlyph(swigCPtr, this);
    return (cPtr == 0) ? null : new ReferenceGlyph(cPtr, false);
  }

  
/**
   * Creates a new {@link LineSegment} object, adds it to the end of the list of
   * curve segment objects of the curve and returns a reference to the
   * newly created object.
   */ public
 LineSegment createLineSegment() {
	return (LineSegment) libsbml.DowncastSBase(libsbmlJNI.GeneralGlyph_createLineSegment(swigCPtr, this), false);
}

  
/**
   * Creates a new {@link CubicBezier} object, adds it to the end of the list of
   * curve segment objects of the curve and returns a reference to the
   * newly created object.
   */ public
 CubicBezier createCubicBezier() {
    long cPtr = libsbmlJNI.GeneralGlyph_createCubicBezier(swigCPtr, this);
    return (cPtr == 0) ? null : new CubicBezier(cPtr, false);
  }

  
/**
   * Remove the reference glyph with the given <code>index</code>.
   * A pointer to the object is returned. If no object has been removed, null
   * is returned.
   */ public
 ReferenceGlyph removeReferenceGlyph(long index) {
    long cPtr = libsbmlJNI.GeneralGlyph_removeReferenceGlyph__SWIG_0(swigCPtr, this, index);
    return (cPtr == 0) ? null : new ReferenceGlyph(cPtr, false);
  }

  
/**
   * Remove the subglyph with the given <code>index</code>.
   * A pointer to the object is returned. If no object has been removed, null
   * is returned.
   */ public
 GraphicalObject removeSubGlyph(long index) {
	return (GraphicalObject) libsbml.DowncastSBase(libsbmlJNI.GeneralGlyph_removeSubGlyph__SWIG_0(swigCPtr, this, index), false);
}

  
/**
   * Remove the reference glyph with the given <code>id</code>.
   * A pointer to the object is returned. If no object has been removed, null
   * is returned.
   */ public
 ReferenceGlyph removeReferenceGlyph(String id) {
    long cPtr = libsbmlJNI.GeneralGlyph_removeReferenceGlyph__SWIG_1(swigCPtr, this, id);
    return (cPtr == 0) ? null : new ReferenceGlyph(cPtr, false);
  }

  
/**
   * Remove the subglyph with the given <code>id</code>.
   * A pointer to the object is returned. If no object has been removed, null
   * is returned.
   */ public
 GraphicalObject removeSubGlyph(String id) {
	return (GraphicalObject) libsbml.DowncastSBase(libsbmlJNI.GeneralGlyph_removeSubGlyph__SWIG_1(swigCPtr, this, id), false);
}

  
/**
   * Returns the index of the reference glyph with the given <code>id</code>.
   * If the glyph does not contain a reference glyph with this
   * id, cpp the
   * value of the maximum long integer is returned as an indicator.
   */ public
 long getIndexForReferenceGlyph(String id) {
    return libsbmlJNI.GeneralGlyph_getIndexForReferenceGlyph(swigCPtr, this, id);
  }

  
/**
   * Returns the index of the subglyph with the given <code>id</code>.
   * If the glyph does not contain a subglyph with this
   * id, cpp the
   * value of the maximum long integer is returned as an indicator.
   */ public
 long getIndexForSubGlyph(String id) {
    return libsbmlJNI.GeneralGlyph_getIndexForSubGlyph(swigCPtr, this, id);
  }

  
/**
   * Returns the XML element name of
   * this SBML object.
   <p>
   * @return the string of the name of this element.
   */ public
 String getElementName() {
    return libsbmlJNI.GeneralGlyph_getElementName(swigCPtr, this);
  }

  
/**
   * Creates and returns a deep copy of this {@link GeneralGlyph}.
   <p>
   * @return a (deep) copy of this {@link GeneralGlyph}.
   */ public
 GeneralGlyph cloneObject() {
    long cPtr = libsbmlJNI.GeneralGlyph_cloneObject(swigCPtr, this);
    return (cPtr == 0) ? null : new GeneralGlyph(cPtr, true);
  }

  
/**
   * Returns the libSBML type code of this object instance.
   <p>
   * <p>
 * LibSBML attaches an identifying code to every kind of SBML object.  These
 * are integer constants known as <em>SBML type codes</em>.  The names of all
 * the codes begin with the characters <code>SBML_</code>.
 * In the Java language interface for libSBML, the
 * type codes are defined as static integer constants in the interface class
 * {@link libsbmlConstants}.    Note that different Level&nbsp;3
 * package plug-ins may use overlapping type codes; to identify the package
 * to which a given object belongs, call the 
 * <code>{@link SBase#getPackageName()}
 * </code>
 * method on the object.
 <p>
 * The exception to this is lists:  all SBML-style list elements have the type 
 * {@link libsbmlConstants#SBML_LIST_OF SBML_LIST_OF}, regardless of what package they 
 * are from.
   <p>
   * @return the SBML type code for this object:
   * {@link libsbmlConstants#SBML_LAYOUT_GENERALGLYPH SBML_LAYOUT_GENERALGLYPH}.
   <p>
   * <p>
 * @warning <span class='warning'>The specific integer values of the possible
 * type codes may be reused by different libSBML plug-ins for SBML Level&nbsp;3.
 * packages,  To fully identify the correct code, <strong>it is necessary to
 * invoke both getPackageName() and getTypeCode()</strong> (or 
 * {@link ListOf#getItemTypeCode()}).</span>
   <p>
   * @see #getElementName()
   * @see #getPackageName()
   */ public
 int getTypeCode() {
    return libsbmlJNI.GeneralGlyph_getTypeCode(swigCPtr, this);
  }

  
/**
    * Creates an {@link XMLNode} object from this.
    */ public
 XMLNode toXML() {
    return new XMLNode(libsbmlJNI.GeneralGlyph_toXML(swigCPtr, this), true);
  }

  public void connectToChild() {
    libsbmlJNI.GeneralGlyph_connectToChild(swigCPtr, this);
  }

  
/** * @internal */ public
 void enablePackageInternal(String pkgURI, String pkgPrefix, boolean flag) {
    libsbmlJNI.GeneralGlyph_enablePackageInternal(swigCPtr, this, pkgURI, pkgPrefix, flag);
  }

}
