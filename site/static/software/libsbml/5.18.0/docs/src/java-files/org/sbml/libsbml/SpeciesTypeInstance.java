/* ----------------------------------------------------------------------------
 * This file was automatically generated by SWIG (http://www.swig.org).
 * Version 3.0.12
 *
 * Do not make changes to this file unless you know what you are doing--modify
 * the SWIG interface file instead.
 * ----------------------------------------------------------------------------- */

package org.sbml.libsbml;

/** 
 * <span class="pkg-marker pkg-color-multi"><a href="group__multi.html">multi</a></span>
 Allows construction of structured {@link MultiSpeciesType} objects.
 <p>
 * The {@link SpeciesTypeInstance} object is a child of {@link MultiSpeciesType}, and
 * provides a way to construct {@link MultiSpeciesType} objects and {@link Species} with
 * multiple components. A {@link MultiSpeciesType} can contain a list of instances of
 * other {@link MultiSpeciesType} objects which can also have their own
 * {@link SpeciesTypeInstance} objects, so the complete construct of a
 * {@link MultiSpeciesType} has a tree structure. A {@link MultiSpeciesType} cannot contain
 * an instance of any other {@link MultiSpeciesType} that already contains the
 * instance of it. In other words, circular references are not allowed when
 * constructing {@link MultiSpeciesType} objects. For example, if a {@link MultiSpeciesType}
 * 'A' contains the instance of another {@link MultiSpeciesType} 'B', 'B' must not
 * contain the instance of 'A' anywhere in the complete structure of 'B'.
 * The optional attribute compartmentReference, of type SIdRef, can be used
 * to indicate which sub-compartment in a composite compartment the
 * {@link SpeciesTypeInstance} is located in.
 */

public class SpeciesTypeInstance extends SBase {
   private long swigCPtr;

   protected SpeciesTypeInstance(long cPtr, boolean cMemoryOwn)
   {
     super(libsbmlJNI.SpeciesTypeInstance_SWIGUpcast(cPtr), cMemoryOwn);
     swigCPtr = cPtr;
   }

   protected static long getCPtr(SpeciesTypeInstance obj)
   {
     return (obj == null) ? 0 : obj.swigCPtr;
   }

   protected static long getCPtrAndDisown (SpeciesTypeInstance obj)
   {
     long ptr = 0;

     if (obj != null)
     {
       ptr             = obj.swigCPtr;
       obj.swigCMemOwn = false;
     }

     return ptr;
   }

  protected void finalize() {
    delete();
  }

  public synchronized void delete() {
    if (swigCPtr != 0) {
      if (swigCMemOwn) {
        swigCMemOwn = false;
        libsbmlJNI.delete_SpeciesTypeInstance(swigCPtr);
      }
      swigCPtr = 0;
    }
    super.delete();
  }

  
/**
   * Creates a new {@link SpeciesTypeInstance} object.
   <p>
   * @param level the SBML Level.
   * @param version the Version within the SBML Level.
   * @param pkgVersion the version of the package.
   <p>
   * <p>
 * @note Attempting to add an object to an {@link SBMLDocument} having a different
 * combination of SBML Level, Version and XML namespaces than the object
 * itself will result in an error at the time a caller attempts to make the
 * addition.  A parent object must have compatible Level, Version and XML
 * namespaces.  (Strictly speaking, a parent may also have more XML
 * namespaces than a child, but the reverse is not permitted.)  The
 * restriction is necessary to ensure that an SBML model has a consistent
 * overall structure.  This requires callers to manage their objects
 * carefully, but the benefit is increased flexibility in how models can be
 * created by permitting callers to create objects bottom-up if desired.  In
 * situations where objects are not yet attached to parents (e.g.,
 * {@link SBMLDocument}), knowledge of the intented SBML Level and Version help
 * libSBML determine such things as whether it is valid to assign a
 * particular value to an attribute.  For packages, this means that the 
 * parent object to which this package element is being added must have
 * been created with the package namespace, or that the package namespace
 * was added to it, even if that parent is not a package object itself.
   */ public
 SpeciesTypeInstance(long level, long version, long pkgVersion) throws org.sbml.libsbml.SBMLConstructorException {
    this(libsbmlJNI.new_SpeciesTypeInstance__SWIG_0(level, version, pkgVersion), true);
  }

  
/**
   * Creates a new {@link SpeciesTypeInstance} object.
   <p>
   * @param level the SBML Level.
   * @param version the Version within the SBML Level.
   * @param pkgVersion the version of the package.
   <p>
   * <p>
 * @note Attempting to add an object to an {@link SBMLDocument} having a different
 * combination of SBML Level, Version and XML namespaces than the object
 * itself will result in an error at the time a caller attempts to make the
 * addition.  A parent object must have compatible Level, Version and XML
 * namespaces.  (Strictly speaking, a parent may also have more XML
 * namespaces than a child, but the reverse is not permitted.)  The
 * restriction is necessary to ensure that an SBML model has a consistent
 * overall structure.  This requires callers to manage their objects
 * carefully, but the benefit is increased flexibility in how models can be
 * created by permitting callers to create objects bottom-up if desired.  In
 * situations where objects are not yet attached to parents (e.g.,
 * {@link SBMLDocument}), knowledge of the intented SBML Level and Version help
 * libSBML determine such things as whether it is valid to assign a
 * particular value to an attribute.  For packages, this means that the 
 * parent object to which this package element is being added must have
 * been created with the package namespace, or that the package namespace
 * was added to it, even if that parent is not a package object itself.
   */ public
 SpeciesTypeInstance(long level, long version) throws org.sbml.libsbml.SBMLConstructorException {
    this(libsbmlJNI.new_SpeciesTypeInstance__SWIG_1(level, version), true);
  }

  
/**
   * Creates a new {@link SpeciesTypeInstance} object.
   <p>
   * @param level the SBML Level.
   * @param version the Version within the SBML Level.
   * @param pkgVersion the version of the package.
   <p>
   * <p>
 * @note Attempting to add an object to an {@link SBMLDocument} having a different
 * combination of SBML Level, Version and XML namespaces than the object
 * itself will result in an error at the time a caller attempts to make the
 * addition.  A parent object must have compatible Level, Version and XML
 * namespaces.  (Strictly speaking, a parent may also have more XML
 * namespaces than a child, but the reverse is not permitted.)  The
 * restriction is necessary to ensure that an SBML model has a consistent
 * overall structure.  This requires callers to manage their objects
 * carefully, but the benefit is increased flexibility in how models can be
 * created by permitting callers to create objects bottom-up if desired.  In
 * situations where objects are not yet attached to parents (e.g.,
 * {@link SBMLDocument}), knowledge of the intented SBML Level and Version help
 * libSBML determine such things as whether it is valid to assign a
 * particular value to an attribute.  For packages, this means that the 
 * parent object to which this package element is being added must have
 * been created with the package namespace, or that the package namespace
 * was added to it, even if that parent is not a package object itself.
   */ public
 SpeciesTypeInstance(long level) throws org.sbml.libsbml.SBMLConstructorException {
    this(libsbmlJNI.new_SpeciesTypeInstance__SWIG_2(level), true);
  }

  
/**
   * Creates a new {@link SpeciesTypeInstance} object.
   <p>
   * @param level the SBML Level.
   * @param version the Version within the SBML Level.
   * @param pkgVersion the version of the package.
   <p>
   * <p>
 * @note Attempting to add an object to an {@link SBMLDocument} having a different
 * combination of SBML Level, Version and XML namespaces than the object
 * itself will result in an error at the time a caller attempts to make the
 * addition.  A parent object must have compatible Level, Version and XML
 * namespaces.  (Strictly speaking, a parent may also have more XML
 * namespaces than a child, but the reverse is not permitted.)  The
 * restriction is necessary to ensure that an SBML model has a consistent
 * overall structure.  This requires callers to manage their objects
 * carefully, but the benefit is increased flexibility in how models can be
 * created by permitting callers to create objects bottom-up if desired.  In
 * situations where objects are not yet attached to parents (e.g.,
 * {@link SBMLDocument}), knowledge of the intented SBML Level and Version help
 * libSBML determine such things as whether it is valid to assign a
 * particular value to an attribute.  For packages, this means that the 
 * parent object to which this package element is being added must have
 * been created with the package namespace, or that the package namespace
 * was added to it, even if that parent is not a package object itself.
   */ public
 SpeciesTypeInstance() throws org.sbml.libsbml.SBMLConstructorException {
    this(libsbmlJNI.new_SpeciesTypeInstance__SWIG_3(), true);
  }

  
/**
   * Creates a new {@link SpeciesTypeInstance} with the given {@link MultiPkgNamespaces}
   * object.
   <p>
   * <p>
 * The package namespaces object used in this constructor is derived from a
 * {@link SBMLNamespaces} object, which encapsulates SBML Level/Version/namespaces
 * information.  It is used to communicate the SBML Level, Version, and 
 * package version and name information used in addition to SBML Level&nbsp;3 Core.  A
 * common approach to using libSBML's {@link SBMLNamespaces} facilities is to create an
 * package namespace object somewhere in a program once, then hand that object
 * as needed to object constructors of that package that accept it as and
 * argument, such as this one.
   <p>
   * @param multins the {@link MultiPkgNamespaces} object
   <p>
   * <p>
 * @note Attempting to add an object to an {@link SBMLDocument} having a different
 * combination of SBML Level, Version and XML namespaces than the object
 * itself will result in an error at the time a caller attempts to make the
 * addition.  A parent object must have compatible Level, Version and XML
 * namespaces.  (Strictly speaking, a parent may also have more XML
 * namespaces than a child, but the reverse is not permitted.)  The
 * restriction is necessary to ensure that an SBML model has a consistent
 * overall structure.  This requires callers to manage their objects
 * carefully, but the benefit is increased flexibility in how models can be
 * created by permitting callers to create objects bottom-up if desired.  In
 * situations where objects are not yet attached to parents (e.g.,
 * {@link SBMLDocument}), knowledge of the intented SBML Level and Version help
 * libSBML determine such things as whether it is valid to assign a
 * particular value to an attribute.  For packages, this means that the 
 * parent object to which this package element is being added must have
 * been created with the package namespace, or that the package namespace
 * was added to it, even if that parent is not a package object itself.
   */ public
 SpeciesTypeInstance(MultiPkgNamespaces multins) throws org.sbml.libsbml.SBMLConstructorException {
    this(libsbmlJNI.new_SpeciesTypeInstance__SWIG_4(MultiPkgNamespaces.getCPtr(multins), multins), true);
  }

  
/**
   * Copy constructor for {@link SpeciesTypeInstance}.
   <p>
   * @param orig the {@link SpeciesTypeInstance} instance to copy.
   */ public
 SpeciesTypeInstance(SpeciesTypeInstance orig) throws org.sbml.libsbml.SBMLConstructorException {
    this(libsbmlJNI.new_SpeciesTypeInstance__SWIG_5(SpeciesTypeInstance.getCPtr(orig), orig), true);
  }

  
/**
   * Creates and returns a deep copy of this {@link SpeciesTypeInstance} object.
   <p>
   * @return a (deep) copy of this {@link SpeciesTypeInstance} object.
   */ public
 SpeciesTypeInstance cloneObject() {
    long cPtr = libsbmlJNI.SpeciesTypeInstance_cloneObject(swigCPtr, this);
    return (cPtr == 0) ? null : new SpeciesTypeInstance(cPtr, true);
  }

  
/**
   * Returns the value of the 'id' attribute of this {@link SpeciesTypeInstance}.
   <p>
   * @return the value of the 'id' attribute of this {@link SpeciesTypeInstance} as a string.
   */ public
 String getId() {
    return libsbmlJNI.SpeciesTypeInstance_getId(swigCPtr, this);
  }

  
/**
   * Returns <code>true</code> if this {@link SpeciesTypeInstance}'s 'id' attribute has been
   * set.
   <p>
   * @return <code>true</code> if this {@link SpeciesTypeInstance}'s 'id' attribute has been
   * set; otherwise, <code>false</code> is returned.
   */ public
 boolean isSetId() {
    return libsbmlJNI.SpeciesTypeInstance_isSetId(swigCPtr, this);
  }

  
/**
   * Sets the value of the 'id' attribute of this {@link SpeciesTypeInstance}.
   <p>
   * @param id String value of the 'id' attribute to be set
   <p>
   * <p>
 * @return integer value indicating success/failure of the
 * function.   The possible values
 * returned by this function are:
   * <ul>
   * <li> {@link libsbmlConstants#LIBSBML_OPERATION_SUCCESS LIBSBML_OPERATION_SUCCESS}
   * <li> {@link libsbmlConstants#LIBSBML_INVALID_ATTRIBUTE_VALUE LIBSBML_INVALID_ATTRIBUTE_VALUE}
   * </ul>
   */ public
 int setId(String id) {
    return libsbmlJNI.SpeciesTypeInstance_setId(swigCPtr, this, id);
  }

  
/**
   * Unsets the value of the 'id' attribute of this {@link SpeciesTypeInstance}.
   <p>
   * <p>
 * @return integer value indicating success/failure of the
 * function.   The possible values
 * returned by this function are:
   * <ul>
   * <li> {@link libsbmlConstants#LIBSBML_OPERATION_SUCCESS LIBSBML_OPERATION_SUCCESS}
   * <li> {@link libsbmlConstants#LIBSBML_OPERATION_FAILED LIBSBML_OPERATION_FAILED}
   * </ul>
   */ public
 int unsetId() {
    return libsbmlJNI.SpeciesTypeInstance_unsetId(swigCPtr, this);
  }

  
/**
   * Returns the value of the 'name' attribute of this {@link SpeciesTypeInstance}.
   <p>
   * @return the value of the 'name' attribute of this {@link SpeciesTypeInstance} as a string.
   */ public
 String getName() {
    return libsbmlJNI.SpeciesTypeInstance_getName(swigCPtr, this);
  }

  
/**
   * Returns <code>true</code> if this {@link SpeciesTypeInstance}'s 'name' attribute has been
   * set.
   <p>
   * @return <code>true</code> if this {@link SpeciesTypeInstance}'s 'name' attribute has been
   * set; otherwise, <code>false</code> is returned.
   */ public
 boolean isSetName() {
    return libsbmlJNI.SpeciesTypeInstance_isSetName(swigCPtr, this);
  }

  
/**
   * Sets the value of the 'name' attribute of this {@link SpeciesTypeInstance}.
   <p>
   * @param name the new value for the attribute.
   <p>
   * <p>
 * @return integer value indicating success/failure of the
 * function.   The possible values
 * returned by this function are:
   * <ul>
   * <li> {@link libsbmlConstants#LIBSBML_OPERATION_SUCCESS LIBSBML_OPERATION_SUCCESS}
   * <li> {@link libsbmlConstants#LIBSBML_INVALID_ATTRIBUTE_VALUE LIBSBML_INVALID_ATTRIBUTE_VALUE}
   * </ul>
   */ public
 int setName(String name) {
    return libsbmlJNI.SpeciesTypeInstance_setName(swigCPtr, this, name);
  }

  
/**
   * Unsets the value of the 'name' attribute of this {@link SpeciesTypeInstance}.
   <p>
   * <p>
 * @return integer value indicating success/failure of the
 * function.   The possible values
 * returned by this function are:
   * <ul>
   * <li> {@link libsbmlConstants#LIBSBML_OPERATION_SUCCESS LIBSBML_OPERATION_SUCCESS}
   * <li> {@link libsbmlConstants#LIBSBML_OPERATION_FAILED LIBSBML_OPERATION_FAILED}
   * </ul>
   */ public
 int unsetName() {
    return libsbmlJNI.SpeciesTypeInstance_unsetName(swigCPtr, this);
  }

  
/**
   * Returns the value of the 'speciesType' attribute of this {@link SpeciesTypeInstance}.
   <p>
   * @return the value of the 'speciesType' attribute of this {@link SpeciesTypeInstance} as a string.
   */ public
 String getSpeciesType() {
    return libsbmlJNI.SpeciesTypeInstance_getSpeciesType(swigCPtr, this);
  }

  
/**
   * Returns <code>true</code> if this {@link SpeciesTypeInstance}'s 'speciesType' attribute
   * has been set.
   <p>
   * @return <code>true</code> if this {@link SpeciesTypeInstance}'s 'speciesType' attribute
   * has been set; otherwise, <code>false</code> is returned.
   */ public
 boolean isSetSpeciesType() {
    return libsbmlJNI.SpeciesTypeInstance_isSetSpeciesType(swigCPtr, this);
  }

  
/**
   * Sets the value of the 'speciesType' attribute of this
   * {@link SpeciesTypeInstance}.
   <p>
   * @param speciesType the new value for the attribute.
   <p>
   * <p>
 * @return integer value indicating success/failure of the
 * function.   The possible values
 * returned by this function are:
   * <ul>
   * <li> {@link libsbmlConstants#LIBSBML_OPERATION_SUCCESS LIBSBML_OPERATION_SUCCESS}
   * <li> {@link libsbmlConstants#LIBSBML_INVALID_ATTRIBUTE_VALUE LIBSBML_INVALID_ATTRIBUTE_VALUE}
   * </ul>
   */ public
 int setSpeciesType(String speciesType) {
    return libsbmlJNI.SpeciesTypeInstance_setSpeciesType(swigCPtr, this, speciesType);
  }

  
/**
   * Unsets the value of the 'speciesType' attribute of this
   * {@link SpeciesTypeInstance}.
   <p>
   * <p>
 * @return integer value indicating success/failure of the
 * function.   The possible values
 * returned by this function are:
   * <ul>
   * <li> {@link libsbmlConstants#LIBSBML_OPERATION_SUCCESS LIBSBML_OPERATION_SUCCESS}
   * <li> {@link libsbmlConstants#LIBSBML_OPERATION_FAILED LIBSBML_OPERATION_FAILED}
   * </ul>
   */ public
 int unsetSpeciesType() {
    return libsbmlJNI.SpeciesTypeInstance_unsetSpeciesType(swigCPtr, this);
  }

  
/**
   * Returns the value of the 'compartmentReference' attribute of this
   * {@link SpeciesTypeInstance}.
   <p>
   * @return the value of the 'compartmentReference' attribute of this
   * {@link SpeciesTypeInstance} as a string.
   */ public
 String getCompartmentReference() {
    return libsbmlJNI.SpeciesTypeInstance_getCompartmentReference(swigCPtr, this);
  }

  
/**
   * Returns <code>true</code> if this {@link SpeciesTypeInstance}'s 'compartmentReference'
   * attribute has been set.
   <p>
   * @return <code>true</code> if this {@link SpeciesTypeInstance}'s 'compartmentReference'
   * attribute has been set; otherwise, <code>false</code> is returned.
   */ public
 boolean isSetCompartmentReference() {
    return libsbmlJNI.SpeciesTypeInstance_isSetCompartmentReference(swigCPtr, this);
  }

  
/**
   * Sets the value of the 'compartmentReference' attribute of this
   * {@link SpeciesTypeInstance}.
   <p>
   * @param compartmentReference the new value for the attribute.
   <p>
   * <p>
 * @return integer value indicating success/failure of the
 * function.   The possible values
 * returned by this function are:
   * <ul>
   * <li> {@link libsbmlConstants#LIBSBML_OPERATION_SUCCESS LIBSBML_OPERATION_SUCCESS}
   * <li> {@link libsbmlConstants#LIBSBML_INVALID_ATTRIBUTE_VALUE LIBSBML_INVALID_ATTRIBUTE_VALUE}
   * </ul>
   */ public
 int setCompartmentReference(String compartmentReference) {
    return libsbmlJNI.SpeciesTypeInstance_setCompartmentReference(swigCPtr, this, compartmentReference);
  }

  
/**
   * Unsets the value of the 'compartmentReference' attribute of this
   * {@link SpeciesTypeInstance}.
   <p>
   * <p>
 * @return integer value indicating success/failure of the
 * function.   The possible values
 * returned by this function are:
   * <ul>
   * <li> {@link libsbmlConstants#LIBSBML_OPERATION_SUCCESS LIBSBML_OPERATION_SUCCESS}
   * <li> {@link libsbmlConstants#LIBSBML_OPERATION_FAILED LIBSBML_OPERATION_FAILED}
   * </ul>
   */ public
 int unsetCompartmentReference() {
    return libsbmlJNI.SpeciesTypeInstance_unsetCompartmentReference(swigCPtr, this);
  }

  
/**
   * Renames all the <code>SIdRef</code> attributes on this element, including any
   * found in MathML content (if such exists).
   <p>
   * This method works by looking at all attributes and (if appropriate)
   * mathematical formulas, comparing the identifiers to the value of 
   * <code>oldid</code>.  If any matches are found, the matching identifiers are replaced
   * with <code>newid</code>.  The method does <em>not</em> descend into child elements.
   <p>
   * @param oldid the old identifier
   * @param newid the new identifier
   */ public
 void renameSIdRefs(String oldid, String newid) {
    libsbmlJNI.SpeciesTypeInstance_renameSIdRefs(swigCPtr, this, oldid, newid);
  }

  
/**
   * Returns the XML element name of this object.
   <p>
   * @return the name of this element, i.e. <code>'speciesTypeInstance'.</code>
   */ public
 String getElementName() {
    return libsbmlJNI.SpeciesTypeInstance_getElementName(swigCPtr, this);
  }

  
/**
   * Returns the libSBML type code for this SBML object.
   <p>
   * <p>
 * LibSBML attaches an identifying code to every kind of SBML object.  These
 * are integer constants known as <em>SBML type codes</em>.  The names of all
 * the codes begin with the characters <code>SBML_</code>.
 * In the Java language interface for libSBML, the
 * type codes are defined as static integer constants in the interface class
 * {@link libsbmlConstants}.    Note that different Level&nbsp;3
 * package plug-ins may use overlapping type codes; to identify the package
 * to which a given object belongs, call the 
 * <code>{@link SBase#getPackageName()}
 * </code>
 * method on the object.
 <p>
 * The exception to this is lists:  all SBML-style list elements have the type 
 * {@link libsbmlConstants#SBML_LIST_OF SBML_LIST_OF}, regardless of what package they 
 * are from.
   <p>
   * @return the SBML type code for this object:
   * {@link libsbmlConstants#SBML_MULTI_BINDING_SITE_SPECIES_TYPE SBML_MULTI_BINDING_SITE_SPECIES_TYPE}.
   <p>
   * <p>
 * @warning <span class='warning'>The specific integer values of the possible
 * type codes may be reused by different libSBML plug-ins for SBML Level&nbsp;3.
 * packages,  To fully identify the correct code, <strong>it is necessary to
 * invoke both getPackageName() and getTypeCode()</strong> (or 
 * {@link ListOf#getItemTypeCode()}).</span>
   <p>
   * @see #getElementName()
   * @see #getPackageName()
   */ public
 int getTypeCode() {
    return libsbmlJNI.SpeciesTypeInstance_getTypeCode(swigCPtr, this);
  }

  
/**
   * Predicate returning <code>true</code> if all the required attributes
   * for this {@link SpeciesTypeInstance} object have been set.
   <p>
   * @note The required attributes for a {@link SpeciesTypeInstance} object are:
   * <ul>
   * <li> 'id'
   * <li> 'speciesType'
   * <li> 'occur'
   *
   * </ul> <p>
   * @return a boolean value indicating whether all the required
   * attributes for this object have been defined.
   */ public
 boolean hasRequiredAttributes() {
    return libsbmlJNI.SpeciesTypeInstance_hasRequiredAttributes(swigCPtr, this);
  }

  
/** * @internal */ public
 void enablePackageInternal(String pkgURI, String pkgPrefix, boolean flag) {
    libsbmlJNI.SpeciesTypeInstance_enablePackageInternal(swigCPtr, this, pkgURI, pkgPrefix, flag);
  }

}
